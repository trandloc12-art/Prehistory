class_name DamageEvent
extends RefCounted

# TODO: 3 biến dữ liệu theo đúng tài liệu — KHÔNG thêm logic xử lý ở đây
# var source        # đối tượng gây damage (Player/Enemy...)
# var target        # đối tượng nhận damage
# var amount: int   # lượng damage

# TODO: hàm khởi tạo nhận đủ 3 giá trị trên và gán vào biến tương ứng
# func _init(source_object, target_object, damage_amount: int) -> void:
#     ...

# Lưu ý khi code thật:
# - Đây chỉ là "gói dữ liệu" (data package), KHÔNG được tự trừ HP ở đây.
# - Không gọi ngược Controller/Model nào cả.
